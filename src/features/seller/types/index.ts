export type Partner = {
  id: string;
  fullName: string;
  phoneNumber: string;
  storeName: string;
};

export type PartnerResponse = {
  id: string;
  subscriptionDate: string;
  user: string;
  phoneNumber: any;
  storeName: string;
};
